backend - fastapi
frontend - react
	styles - materialize

in backend map we have:
	env map witch represent the virtual_invironment for backend,
	and src map - where is all the backend code

in frontend map we have the quiz map that represent the react project
	

to run:
	activate venv from back, then being venv go to front and run "npm run dev"